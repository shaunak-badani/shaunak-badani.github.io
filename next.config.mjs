/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "shaunakbadani.github.io",
    images: {
        unoptimized: true
    }
};

export default nextConfig;
